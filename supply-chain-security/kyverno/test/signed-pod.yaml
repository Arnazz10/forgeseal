apiVersion: v1
kind: Pod
metadata:
  name: signed-test
  labels:
    app: signed-test
  annotations:
    supply-chain/sbom: "s3://my-bucket/sboms/checkout-svc-${GITHUB_SHA}.json"
    supply-chain/provenance: "s3://my-bucket/provenance/checkout-svc-${GITHUB_SHA}.intoto.jsonl"
spec:
  containers:
  - name: signed
    image: REPLACE_WITH_SIGNED_IMAGE
    command: ["sleep", "3600"]
